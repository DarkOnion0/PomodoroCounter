[package]
name = "cli"
version = "0.1.0"
edition = "2021"
icense = "AGPL-3.0-only"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
clap = { version = "4.0.30", features = ["derive"] }
pomolib = { path = "../pomolib" }
